<form method="post" action="{{ path('app_ressource_delete', {'id': ressource.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');" class="mt-4">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ ressource.id) }}">
    <button type="submit" class="btn bg-red-500 text-white p-2 rounded-md hover:bg-red-600 focus:outline-none">Delete</button>
</form>
