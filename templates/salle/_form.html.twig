{{ form_start(form) }}
    {{ form_widget(form, {'attr': {'class': 'w-full gap-2 p-2 mb-4 border rounded-md'}}) }}
<button class="btn bg-blue-500 text-white p-2 rounded-md hover:bg-blue-600 focus:outline-none">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}
