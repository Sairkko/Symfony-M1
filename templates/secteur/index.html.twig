{% extends 'base.html.twig' %}

{% block title %}Secteur index{% endblock %}

{% block body %}
    <div class="max-w-2xl mx-auto mt-8 p-6 bg-white rounded-md shadow-md">
        <h1 class="text-2xl font-semibold mb-6">Secteur index</h1>

        <table class="table w-full border-collapse">
            <thead>
            <tr class="bg-gray-200">
                <th class="py-2 px-4 border">Id</th>
                <th class="py-2 px-4 border">Description</th>
                <th class="py-2 px-4 border">Nom</th>
                <th class="py-2 px-4 border">Actions</th>
            </tr>
            </thead>
            <tbody>
            {% for secteur in secteurs %}
                <tr class="border-b">
                    <td class="py-2 px-4 border">{{ secteur.id }}</td>
                    <td class="py-2 px-4 border">{{ secteur.description }}</td>
                    <td class="py-2 px-4 border">{{ secteur.nom }}</td>
                    <td class="py-2 px-4 border">
                        <a href="{{ path('app_secteur_show', {'id': secteur.id}) }}" class="text-blue-500 hover:underline">show</a>
                        <a href="{{ path('app_secteur_edit', {'id': secteur.id}) }}" class="text-yellow-500 hover:underline">edit</a>
                    </td>
                </tr>
            {% else %}
                <tr>
                    <td class="py-2 px-4 border" colspan="4">No records found</td>
                </tr>
            {% endfor %}
            </tbody>
        </table>

        <a href="{{ path('app_secteur_new') }}" class="block mt-4 text-blue-500 hover:underline">Create new</a>
    </div>
{% endblock %}
