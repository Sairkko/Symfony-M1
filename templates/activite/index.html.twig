{% extends 'base.html.twig' %}

{% block title %}Activite index{% endblock %}

{% block body %}
    <div class="max-w-2xl mx-auto mt-8 p-6 bg-white rounded-md shadow-md">
        <h1 class="text-2xl font-semibold mb-6">Activite index</h1>

        <table class="table w-full border-collapse">
            <thead>
            <tr class="bg-gray-200">
                <th class="py-2 px-4 border">Id</th>
                <th class="py-2 px-4 border">Nom</th>
                <th class="py-2 px-4 border">Actions</th>
            </tr>
            </thead>
            <tbody>
            {% for activite in activites %}
                <tr class="border-b">
                    <td class="py-2 px-4 border">{{ activite.id }}</td>
                    <td class="py-2 px-4 border">{{ activite.nom }}</td>
                    <td class="py-2 px-4 border">
                        <a href="{{ path('app_activite_show', {'id': activite.id}) }}" class="text-blue-500 hover:underline">Show</a>
                        <a href="{{ path('app_activite_edit', {'id': activite.id}) }}" class="text-yellow-500 hover:underline">Edit</a>
                    </td>
                </tr>
            {% else %}
                <tr>
                    <td class="py-2 px-4 border" colspan="3">No records found</td>
                </tr>
            {% endfor %}
            </tbody>
        </table>

        <a href="{{ path('app_activite_new') }}" class="block mt-4 text-blue-500 hover:underline">Create new</a>
    </div>
{% endblock %}
